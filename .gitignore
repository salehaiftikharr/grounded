node_modules
.env
.env.local
*.tsbuildinfo

# Next.js
/.next
/out
next-env.d.ts

# the CLI's local index is a build artifact (data/index.json IS committed for the web demo)
/index.json

# macOS
.DS_Store
